#ifndef IMU_H
#define IMU_H

class IMU {
  public:
    IMU(const int pin); 
    // no idea how this works, can put in functions later

  private:
    // helper functions/variables if necessary
};

#endif